require 'spec_helper'

describe WebhookUp do
  it 'has a version number' do
    expect(WebhookUp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
